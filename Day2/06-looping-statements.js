// For loop

for(let index=1; index<=10; index++) 
{
console.log(index);
}

// For each

let automationTools = ["Playwright","Selenium","Puppeteer"]
automationTools.forEach(function(index1){
console.log(index1);

 })

// while loop to print values for a perticular range

let count = 10;
while(count){       // if the condition is truthy it will go inside the block
                    // if the condition is falsy it will not go inside the block
    console.log(count);
    count--   //0= falsy
}

// let num = 10;

// do(num){
//     console.log(num)
// } 

